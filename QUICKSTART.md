# Quick Start Guide

## 🚀 Быстрый старт интернет-магазина

### Шаг 1: Подготовка базы данных
```bash
# Выполнить миграции (создать таблицы)
php artisan migrate

# (Опционально) Заполнить тестовыми данными
php artisan db:seed
```

### Шаг 2: Установка JavaScript зависимостей
```bash
npm install
```

### Шаг 3: Запуск в режиме разработки

**Вариант 1: Параллельный запуск (рекомендуется)**
```bash
# Терминал 1 - Laravel dev server
php artisan serve

# Терминал 2 - Vite dev server (watch mode)
npm run dev
```

**Вариант 2: Только продакшен сборка**
```bash
npm run build
```

### Шаг 4: Открыть приложение
```
http://localhost:8000
```

## 📁 Структура файлов

```
├── app/
│   ├── Http/
│   │   ├── Controllers/
│   │   │   ├── ItemController.php        ← Товары
│   │   │   ├── CategoryController.php    ← Категории
│   │   │   ├── CartController.php        ← Корзина
│   │   │   ├── OrderController.php       ← Заказы
│   │   │   ├── ReviewController.php      ← Отзывы
│   │   │   └── WishController.php        ← Список желаний
│   │   └── Middleware/
│   │       └── HandleInertiaRequests.php ← Inertia middleware
│   ├── Models/                           ← Модели БД
│   └── Policies/
│       ├── OrderPolicy.php
│       └── ReviewPolicy.php
│
├── resources/
│   ├── js/
│   │   ├── app.jsx                       ← Entry point
│   │   ├── Layouts/
│   │   │   └── Layout.jsx                ← Main layout
│   │   └── Pages/                        ← Inertia компоненты
│   │       ├── Items/
│   │       ├── Cart/
│   │       ├── Orders/
│   │       ├── Reviews/
│   │       ├── Wishes/
│   │       └── Categories/
│   ├── css/
│   │   └── app.css                       ← Tailwind styles
│   └── views/
│       ├── app.blade.php                 ← HTML template
│       └── welcome.blade.php
│
├── routes/
│   └── web.php                           ← Маршруты
│
├── database/
│   └── migrations/                       ← Миграции БД
│
├── vite.config.js                        ← Vite конфигурация
└── tailwind.config.js                    ← Tailwind конфигурация
```

## 🔑 Основные команды

```bash
# Laravel команды
php artisan serve              # Запуск dev сервера
php artisan tinker             # REPL для экспериментов
php artisan migrate            # Запустить миграции
php artisan db:seed            # Заполнить БД тестовыми данными

# NPM команды
npm run dev                    # Vite в режиме разработки
npm run build                  # Сборка для production
npm run lint                   # Проверка кода (если настроено)
```

## 🎨 Основные технологии

| Технология | Версия | Назначение |
|------------|--------|-----------|
| Laravel | 11 | Backend framework |
| Inertia.js | Latest | Монолитный JS framework |
| React | 18+ | UI библиотека |
| Tailwind CSS | 3 | Стилизация |
| Vite | Latest | Bundler |

## 🔐 Авторизация

Приложение использует встроенную авторизацию Laravel.

**Для регистрации:**
1. Откройте `/register`
2. Введите данные
3. Нажмите "Регистрация"

**Для входа:**
1. Откройте `/login`
2. Введите email и пароль
3. Нажмите "Вход"

## 🛍️ Использование магазина

### Для гостей:
- ✓ Просмотр товаров
- ✓ Поиск и фильтрация
- ✓ Добавление в корзину
- ✗ Оформление заказа (требуется вход)
- ✗ Отзывы (требуется вход)
- ✗ Список желаний (требуется вход)

### Для авторизованных:
- ✓ Все функции гостя
- ✓ Оформление заказа
- ✓ История заказов
- ✓ Написание отзывов
- ✓ Список желаний
- ✓ Управление аккаунтом

## 🔗 API Examples

### 1. Получить все товары
```javascript
GET /items
```

### 2. Поиск товаров
```javascript
GET /items/search?q=query&sort=latest
```

### 3. Добавить в корзину
```javascript
POST /cart/add/{item_id}
```

### 4. Оформить заказ
```javascript
POST /orders
```

## 🐛 Решение проблем

### Ошибка: "CSRF token mismatch"
- Убедитесь, что форма содержит CSRF token
- Проверьте session конфигурацию

### Ошибка: "Route not found"
- Проверьте routes/web.php
- Убедитесь, что контроллер зарегистрирован

### Ошибка: "Component not found"
- Проверьте путь к компоненту в Pages/
- Убедитесь, что название соответствует (case-sensitive)

### Vite не обновляет изменения
- Проверьте, что npm run dev запущен
- Очистите кеш браузера (Ctrl+Shift+Delete)
- Перезагрузите страницу (F5)

## 📚 Дополнительные ресурсы

- [Laravel документация](https://laravel.com/docs)
- [Inertia.js документация](https://inertiajs.com)
- [React документация](https://react.dev)
- [Tailwind CSS документация](https://tailwindcss.com)

## ✅ Чеклист при развертывании

- [ ] Создана база данных
- [ ] Установлены PHP зависимости (`composer install`)
- [ ] Установлены JavaScript зависимости (`npm install`)
- [ ] Запущены миграции (`php artisan migrate`)
- [ ] Сгенерирован APP_KEY (`php artisan key:generate`)
- [ ] Создан `.env` файл на основе `.env.example`
- [ ] Настроена база данных в `.env`
- [ ] Загружены пример данные (опционально)
- [ ] Построены ассеты для production (`npm run build`)

## 📝 Заметки

1. **Сеансы корзины**: Гостевые корзины сохраняются по session_id
2. **Авторизация заказов**: Только владелец может видеть свой заказ
3. **Отзывы**: Один пользователь = один отзыв на товар
4. **Статусы**: pending (ожидание) → completed (завершен) или cancelled (отменен)

---

Готово! Приложение полностью настроено и готово к использованию. 🎉
